---
ms.date: 08/24/2018
keywords: rutiny prostředí PowerShell
title: Učení Powershellových názvů
ms.assetid: b4d0fd22-8298-4ee6-82ae-9b6f2907c986
ms.openlocfilehash: d4e374530c8628df0d53fd860c4b7a149c58eb60
ms.sourcegitcommit: 59727f71dc204785a1bcdedc02716d8340a77aeb
ms.translationtype: MT
ms.contentlocale: cs-CZ
ms.lasthandoff: 08/28/2018
ms.locfileid: "43134199"
---
# <a name="learning-powershell-names"></a><span data-ttu-id="c2534-103">Učení Powershellových názvů</span><span class="sxs-lookup"><span data-stu-id="c2534-103">Learning PowerShell names</span></span>

<span data-ttu-id="c2534-104">Učení názvů příkazů a parametry vyžaduje spoustu času investici se většina rozhraní příkazového řádku.</span><span class="sxs-lookup"><span data-stu-id="c2534-104">Learning names of commands and parameters requires a significant time investment with most command-line interfaces.</span></span> <span data-ttu-id="c2534-105">Problém je, že existuje několik vzorových postupů.</span><span class="sxs-lookup"><span data-stu-id="c2534-105">The issue is that there are few patterns.</span></span> <span data-ttu-id="c2534-106">Zapamatování je pouze způsob, jak další příkazy a parametry, které je třeba použít v pravidelných intervalech.</span><span class="sxs-lookup"><span data-stu-id="c2534-106">Memorization is only way to learn the commands and parameters that you need to use on a regular basis.</span></span>

<span data-ttu-id="c2534-107">Při práci s nový příkaz nebo parametr vždy nelze použít, co už znáte.</span><span class="sxs-lookup"><span data-stu-id="c2534-107">When you work with a new command or parameter, you can't always use what you already know.</span></span> <span data-ttu-id="c2534-108">Je nutné najít a zjistěte, nový název.</span><span class="sxs-lookup"><span data-stu-id="c2534-108">You have to find and learn a new name.</span></span> <span data-ttu-id="c2534-109">Rozhraní příkazového řádku tradičně, začněte s malou sadu nástrojů a růst s přírůstkové doplňky.</span><span class="sxs-lookup"><span data-stu-id="c2534-109">Traditionally, command-line interfaces start with a small set of tools and grow with incremental additions.</span></span> <span data-ttu-id="c2534-110">Je snadno zjistit proto neexistuje žádný standardní struktury.</span><span class="sxs-lookup"><span data-stu-id="c2534-110">It's easy to see why there's no standard structure.</span></span>
<span data-ttu-id="c2534-111">Vypadá to, že logické pro názvy příkazů vzhledem k tomu, že každý příkaz je samostatný nástroj.</span><span class="sxs-lookup"><span data-stu-id="c2534-111">This seems logical for command names since each command is a separate tool.</span></span> <span data-ttu-id="c2534-112">Prostředí PowerShell má lepší způsob, jak zpracovat názvy příkazů.</span><span class="sxs-lookup"><span data-stu-id="c2534-112">PowerShell has a better way to handle command names.</span></span>

## <a name="learning-command-names-in-traditional-shells"></a><span data-ttu-id="c2534-113">Učení názvů příkazů v tradiční prostředí</span><span class="sxs-lookup"><span data-stu-id="c2534-113">Learning command names in traditional shells</span></span>

<span data-ttu-id="c2534-114">Většina příkazů se za účelem správy prvky operačních systémech a aplikacích, jako je například služeb nebo procesů.</span><span class="sxs-lookup"><span data-stu-id="c2534-114">Most commands are built to manage elements of the operating system or applications, such as services or processes.</span></span> <span data-ttu-id="c2534-115">Příkazy mají názvy, které můžou nebo nemusí vejde do rodiny.</span><span class="sxs-lookup"><span data-stu-id="c2534-115">The commands have names that may or may not fit into a family.</span></span> <span data-ttu-id="c2534-116">Například v systémech Windows, můžete použít `net start` a `net stop` příkazy ke spuštění a zastavení služby.</span><span class="sxs-lookup"><span data-stu-id="c2534-116">For example, on Windows systems, you can use the `net start` and `net stop` commands to start and stop a service.</span></span> <span data-ttu-id="c2534-117">**RŮZNÉ Soubor EXE** je jiný nástroj pro ovládací prvek služby pro Windows.</span><span class="sxs-lookup"><span data-stu-id="c2534-117">**SC.EXE** is another service control tool for Windows.</span></span> <span data-ttu-id="c2534-118">Tento název se nevejde do vzor pojmenování `net` služby příkazy.</span><span class="sxs-lookup"><span data-stu-id="c2534-118">That name does not fit into the naming pattern for the `net` service commands.</span></span> <span data-ttu-id="c2534-119">Pro řízení procesů, má Windows `tasklist` příkaz listovat procesy a `taskkill` příkazu ukončit.</span><span class="sxs-lookup"><span data-stu-id="c2534-119">For process management, Windows has the `tasklist` command to list processes and the `taskkill` command to kill processes.</span></span>

<span data-ttu-id="c2534-120">Tyto příkazy mají navíc specifikace nestandardní parametru.</span><span class="sxs-lookup"><span data-stu-id="c2534-120">Also, these commands have irregular parameter specifications.</span></span> <span data-ttu-id="c2534-121">Nelze použít `net start` příkaz pro spuštění služby ve vzdáleném počítači.</span><span class="sxs-lookup"><span data-stu-id="c2534-121">You can't use the `net start` command to start a service on a remote computer.</span></span> <span data-ttu-id="c2534-122">`sc` Příkaz službu můžete spustit na vzdáleném počítači.</span><span class="sxs-lookup"><span data-stu-id="c2534-122">The `sc` command can start a service on a remote computer.</span></span>
<span data-ttu-id="c2534-123">Ale pokud chcete zadat vzdálený počítač, musíte přidat předponu stejný název jako dvojité zpětné lomítko.</span><span class="sxs-lookup"><span data-stu-id="c2534-123">But to specify the remote computer, you must prefix its name with a double backslash.</span></span> <span data-ttu-id="c2534-124">Chcete-li spustit službu zařazování tisku ve vzdáleném počítači s názvem DC01, zadejte `sc \\DC01 start spooler`.</span><span class="sxs-lookup"><span data-stu-id="c2534-124">To start the spooler service on a remote computer named DC01, you type `sc \\DC01 start spooler`.</span></span> <span data-ttu-id="c2534-125">Do seznamu Úkoly spuštěné v DC01 použijete **/S** parametr a název počítače bez zpětná lomítka.</span><span class="sxs-lookup"><span data-stu-id="c2534-125">To list tasks running on DC01, you use the **/S** parameter and the computer name without backslashes.</span></span> <span data-ttu-id="c2534-126">Například `tasklist /S DC01`.</span><span class="sxs-lookup"><span data-stu-id="c2534-126">For example, `tasklist /S DC01`.</span></span>

<span data-ttu-id="c2534-127">Příklady prvků spravovat počítače, které mají jasně definovaných životní cykly jsou služby a procesy.</span><span class="sxs-lookup"><span data-stu-id="c2534-127">Services and processes are examples of manageable elements on a computer that have well-defined life cycles.</span></span> <span data-ttu-id="c2534-128">Můžete spustit nebo zastavit služby a procesy nebo získat seznam všech aktuálně spuštěné služby nebo procesy.</span><span class="sxs-lookup"><span data-stu-id="c2534-128">You may start or stop services and processes, or get a list of all currently running services or processes.</span></span> <span data-ttu-id="c2534-129">I když existují důležité technické rozdíly mezi nimi, akce, které můžete provádět na službách a procesy jsou koncepčně stejné.</span><span class="sxs-lookup"><span data-stu-id="c2534-129">Although there are important technical distinctions between them, the actions you perform on services and processes are conceptually the same.</span></span> <span data-ttu-id="c2534-130">Kromě toho volby, které provedeme přizpůsobit tak, že zadáte parametry akce může být koncepčně podobné také.</span><span class="sxs-lookup"><span data-stu-id="c2534-130">Furthermore, the choices we make to customize an action by specifying parameters may be conceptually similar as well.</span></span>

<span data-ttu-id="c2534-131">Powershellu zneužívá tyto podobnosti a snížit počet různých názvů, potřebujete pochopit a používat rutiny.</span><span class="sxs-lookup"><span data-stu-id="c2534-131">PowerShell exploits these similarities to reduce the number of distinct names you need to know to understand and use cmdlets.</span></span>

## <a name="cmdlets-use-verb-noun-names-to-reduce-command-memorization"></a><span data-ttu-id="c2534-132">Rutiny využít k omezení příkaz zapamatování názvy sloveso-podstatné jméno</span><span class="sxs-lookup"><span data-stu-id="c2534-132">Cmdlets use verb-noun names to reduce command memorization</span></span>

<span data-ttu-id="c2534-133">Prostředí PowerShell používá systém pojmenování "sloveso-podstatné jméno".</span><span class="sxs-lookup"><span data-stu-id="c2534-133">PowerShell uses a "verb-noun" naming system.</span></span> <span data-ttu-id="c2534-134">Každý název rutiny se skládá z standardní operace dělení s konkrétní podstatné jméno.</span><span class="sxs-lookup"><span data-stu-id="c2534-134">Each cmdlet name consists of a standard verb hyphenated with a specific noun.</span></span> <span data-ttu-id="c2534-135">Příkazy prostředí PowerShell nejsou vždy anglické příkazy, ale jejich expresní určitých akcí v prostředí PowerShell.</span><span class="sxs-lookup"><span data-stu-id="c2534-135">PowerShell verbs are not always English verbs, but they express specific actions in PowerShell.</span></span> <span data-ttu-id="c2534-136">S podstatnými jmény jsou velmi podobně jako podstatná jména v jakémkoli jazyce.</span><span class="sxs-lookup"><span data-stu-id="c2534-136">Nouns are very much like nouns in any language.</span></span> <span data-ttu-id="c2534-137">Popisují konkrétní typy objektů, které jsou důležité pro správu systému.</span><span class="sxs-lookup"><span data-stu-id="c2534-137">They describe specific types of objects that are important in system administration.</span></span> <span data-ttu-id="c2534-138">Je snadné ukazují, jak tyto názvy dvou částí úsilí learning pohledem na několik příkladů.</span><span class="sxs-lookup"><span data-stu-id="c2534-138">It's easy to demonstrate how these two-part names reduce learning effort by looking at a few examples.</span></span>

<span data-ttu-id="c2534-139">PowerShell je doporučená sada standardních operací.</span><span class="sxs-lookup"><span data-stu-id="c2534-139">PowerShell has a recommended set of standard verbs.</span></span> <span data-ttu-id="c2534-140">Podstatná jména jsou méně s omezeným přístupem, ale vždy popisují příkaz postupuje podle.</span><span class="sxs-lookup"><span data-stu-id="c2534-140">Nouns are less restricted, but always describe what the verb acts upon.</span></span> <span data-ttu-id="c2534-141">Prostředí PowerShell, jako má příkazy `Get-Process`, `Stop-Process`, `Get-Service`, a `Stop-Service`.</span><span class="sxs-lookup"><span data-stu-id="c2534-141">PowerShell has commands such as `Get-Process`, `Stop-Process`, `Get-Service`, and `Stop-Service`.</span></span>

<span data-ttu-id="c2534-142">Tento příklad dvou podstatná jména a příkazy konzistence není zjednodušit učení této snazší.</span><span class="sxs-lookup"><span data-stu-id="c2534-142">For this example of two nouns and verbs, consistency does not simplify learning that much.</span></span> <span data-ttu-id="c2534-143">Rozšíření seznamu standardizované sadu 10 příkazy a 10 podstatná jména.</span><span class="sxs-lookup"><span data-stu-id="c2534-143">Extend that list to a standardized set of 10 verbs and 10 nouns.</span></span> <span data-ttu-id="c2534-144">Nyní máte jenom 20 slova, která chcete pochopit.</span><span class="sxs-lookup"><span data-stu-id="c2534-144">Now you only have 20 words to understand.</span></span>
<span data-ttu-id="c2534-145">Ale tato slova mohou být kombinovány pro názvy formuláře 100 různých příkazů.</span><span class="sxs-lookup"><span data-stu-id="c2534-145">But those words can be combined to form 100 distinct command names.</span></span>

<span data-ttu-id="c2534-146">Je snadno pochopit, co dělá příkaz prostředí PowerShell najdete jeho název.</span><span class="sxs-lookup"><span data-stu-id="c2534-146">It's easy to understand what a PowerShell command does by reading its name.</span></span> <span data-ttu-id="c2534-147">Vypnout počítač pomocí příkazu `Stop-Computer`.</span><span class="sxs-lookup"><span data-stu-id="c2534-147">The command to shut down a computer is `Stop-Computer`.</span></span> <span data-ttu-id="c2534-148">Seznam všech počítačů v síti pomocí příkazu `Get-Computer`.</span><span class="sxs-lookup"><span data-stu-id="c2534-148">The command to list all computers on a network is `Get-Computer`.</span></span>
<span data-ttu-id="c2534-149">Pomocí příkazu získat systémové datum `Get-Date`.</span><span class="sxs-lookup"><span data-stu-id="c2534-149">The command to get the system date is `Get-Date`.</span></span>

<span data-ttu-id="c2534-150">Můžete zobrazit seznam všech příkazů, které obsahují konkrétní operaci s **příkaz** parametr pro `Get-Command`.</span><span class="sxs-lookup"><span data-stu-id="c2534-150">You can list all commands that include a particular verb with the **Verb** parameter for `Get-Command`.</span></span> <span data-ttu-id="c2534-151">Například, pokud chcete zobrazit všechny rutiny, které používají příkaz `Get`, typ:</span><span class="sxs-lookup"><span data-stu-id="c2534-151">For example, to see all cmdlets that use the verb `Get`, type:</span></span>

```
PS> Get-Command -Verb Get

CommandType     Name                            Definition
-----------     ----                            ----------
Cmdlet          Get-Acl                         Get-Acl [[-Path] <String[]>]...
Cmdlet          Get-Alias                       Get-Alias [[-Name] <String[]...
Cmdlet          Get-AuthenticodeSignature       Get-AuthenticodeSignature [-...
Cmdlet          Get-ChildItem                   Get-ChildItem [[-Path] <Stri...
...
```

<span data-ttu-id="c2534-152">Použití **podstatné jméno** parametr zobrazíte řadu příkazů, které mají vliv stejný typ objektu.</span><span class="sxs-lookup"><span data-stu-id="c2534-152">Use the **Noun** parameter to see a family of commands that affect the same type of object.</span></span> <span data-ttu-id="c2534-153">Například spusťte následující příkaz zobrazí příkazy, které jsou k dispozici pro správu služeb:</span><span class="sxs-lookup"><span data-stu-id="c2534-153">For example, run following command to see the commands  available for managing services:</span></span>

```
PS> Get-Command -Noun Service

CommandType     Name                            Definition
-----------     ----                            ----------
Cmdlet          Get-Service                     Get-Service [[-Name] <String...
Cmdlet          New-Service                     New-Service [-Name] <String>...
Cmdlet          Restart-Service                 Restart-Service [-Name] <Str...
Cmdlet          Resume-Service                  Resume-Service [-Name] <Stri...
Cmdlet          Set-Service                     Set-Service [-Name] <String>...
Cmdlet          Start-Service                   Start-Service [-Name] <Strin...
Cmdlet          Stop-Service                    Stop-Service [-Name] <String...
Cmdlet          Suspend-Service                 Suspend-Service [-Name] <Str...
...
```

## <a name="cmdlets-use-standard-parameters"></a><span data-ttu-id="c2534-154">Rutiny používají standardní parametry</span><span class="sxs-lookup"><span data-stu-id="c2534-154">Cmdlets use standard parameters</span></span>

<span data-ttu-id="c2534-155">Jak bylo uvedeno dříve, příkazech použitých v tradiční rozhraní příkazového řádku vždy mají názvy parametrů konzistentní vzhledem k aplikacím.</span><span class="sxs-lookup"><span data-stu-id="c2534-155">As noted earlier, commands used in traditional command-line interfaces don't always have consistent parameter names.</span></span> <span data-ttu-id="c2534-156">Parametry jsou často jedním znakem nebo se používá zkratka slova, která se dají snadno na typ, ale nejsou snadno srozumitelné pro nové uživatele.</span><span class="sxs-lookup"><span data-stu-id="c2534-156">Parameters are often single-character or abbreviated words that are easy to type but aren't easily understood by new users.</span></span>

<span data-ttu-id="c2534-157">Na rozdíl od většina jiných tradiční rozhraní příkazového řádku Powershellu přímo zpracovává parametrů a používá tento přímý přístup k parametrům společně s pokyny pro vývojáře ke standardizaci názvy parametrů.</span><span class="sxs-lookup"><span data-stu-id="c2534-157">Unlike most other traditional command-line interfaces, PowerShell processes parameters directly, and it uses this direct access to the parameters along with developer guidance to standardize parameter names.</span></span> <span data-ttu-id="c2534-158">Tyto doprovodné materiály může vést ke vzniku, ale nezaručuje, že každou rutinu odpovídá standardu.</span><span class="sxs-lookup"><span data-stu-id="c2534-158">This guidance encourages but does not guarantee that every cmdlet conforms to the standard.</span></span>

<span data-ttu-id="c2534-159">Prostředí PowerShell také standardizuje parametr oddělovače.</span><span class="sxs-lookup"><span data-stu-id="c2534-159">PowerShell also standardizes the parameter separator.</span></span> <span data-ttu-id="c2534-160">Názvy parametrů mít vždy "-" před pomocí příkazu prostředí PowerShell.</span><span class="sxs-lookup"><span data-stu-id="c2534-160">Parameter names always have a '-' prepended to them with a PowerShell command.</span></span> <span data-ttu-id="c2534-161">Vezměte v úvahu v následujícím příkladu:</span><span class="sxs-lookup"><span data-stu-id="c2534-161">Consider the following example:</span></span>

```powershell
Get-Command -Name Clear-Host
```

<span data-ttu-id="c2534-162">Název parametru je **název**, ale je typovaný jako `-Name` při použití v příkazovém řádku jako parametr.</span><span class="sxs-lookup"><span data-stu-id="c2534-162">The parameter's name is **Name**, but it is typed as `-Name` when used on the command line as a parameter.</span></span>

<span data-ttu-id="c2534-163">Tady jsou některé obecné charakteristiky standardní parametr názvy a použití.</span><span class="sxs-lookup"><span data-stu-id="c2534-163">Here are some of the general characteristics of the standard parameter names and usages.</span></span>

### <a name="the-help-parameter-"></a><span data-ttu-id="c2534-164">Parametr nápovědy (?)</span><span class="sxs-lookup"><span data-stu-id="c2534-164">The Help parameter (?)</span></span>

<span data-ttu-id="c2534-165">Pokud zadáte `-Help` nebo `-?` parametru u libovolné rutiny prostředí PowerShell zobrazí nápovědu pro rutinu.</span><span class="sxs-lookup"><span data-stu-id="c2534-165">When you specify the `-Help` or `-?` parameter on any cmdlet, PowerShell displays help for the cmdlet.</span></span> <span data-ttu-id="c2534-166">Rutina není spuštěn.</span><span class="sxs-lookup"><span data-stu-id="c2534-166">The cmdlet is not executed.</span></span>

### <a name="common-parameters"></a><span data-ttu-id="c2534-167">Společné parametry</span><span class="sxs-lookup"><span data-stu-id="c2534-167">Common parameters</span></span>

<span data-ttu-id="c2534-168">Prostředí PowerShell víme o několika *společné parametry*.</span><span class="sxs-lookup"><span data-stu-id="c2534-168">PowerShell has several *common parameters*.</span></span> <span data-ttu-id="c2534-169">Tyto parametry se řídí modul prostředí PowerShell.</span><span class="sxs-lookup"><span data-stu-id="c2534-169">These parameters are controlled by the PowerShell engine.</span></span> <span data-ttu-id="c2534-170">Společné parametry vždy chovají stejným způsobem.</span><span class="sxs-lookup"><span data-stu-id="c2534-170">Common parameters always behave the same way.</span></span> <span data-ttu-id="c2534-171">Společné parametry jsou **WhatIf**, **potvrdit**, **Verbose**, **ladění**, **upozornit**, **ErrorAction**, **ErrorVariable**, **OutVariable**, a **OutBuffer**.</span><span class="sxs-lookup"><span data-stu-id="c2534-171">The common parameters are **WhatIf**, **Confirm**, **Verbose**, **Debug**, **Warn**, **ErrorAction**, **ErrorVariable**, **OutVariable**, and **OutBuffer**.</span></span>

### <a name="recommended-parameter-names"></a><span data-ttu-id="c2534-172">Názvy parametrů doporučené</span><span class="sxs-lookup"><span data-stu-id="c2534-172">Recommended parameter names</span></span>

<span data-ttu-id="c2534-173">Rutiny prostředí PowerShell core použít standardní názvy pro podobně jako parametry.</span><span class="sxs-lookup"><span data-stu-id="c2534-173">The PowerShell core cmdlets use standard names for similar parameters.</span></span> <span data-ttu-id="c2534-174">Použijte tyto standardní názvy nevynucuje, ale neexistuje explicitní pokyny, které podpořit standardizaci.</span><span class="sxs-lookup"><span data-stu-id="c2534-174">The use of these standard names is not enforced, but there is explicit guidance to encourage standardization.</span></span>

<span data-ttu-id="c2534-175">Doporučený název parametru, který odkazuje na počítači je třeba, **ComputerName**, nikoli serveru, hostitele, System, uzel nebo Další běžnou alternativou.</span><span class="sxs-lookup"><span data-stu-id="c2534-175">For example, the recommended name for a parameter that refers to a computer is **ComputerName**, rather than Server, Host, System, Node, or some other common alternative.</span></span> <span data-ttu-id="c2534-176">Další důležité doporučené parametr názvy jsou **platnost**, **vyloučit**, **zahrnout**, **PassThru**, **cesta**, a **CaseSensitive**.</span><span class="sxs-lookup"><span data-stu-id="c2534-176">Other important recommended parameter names are **Force**, **Exclude**, **Include**, **PassThru**, **Path**, and **CaseSensitive**.</span></span>